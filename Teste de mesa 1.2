using System;

class Program
{
    static void Main()
    {
        int[] v = new int[7];
        int a = 2;

        while (a < 6) 
        {
            v[a] = 10 * a;
            a += 1;
        }

        for (int x = 0; x < 7; x++)
        {
            Console.WriteLine($"Lugar: {x} a resposta é: {v[x]}");
        }
    }
}
